{
  "mcpServers": {
    "rescuetime": {
      "command": "fastmcp",
      "args": ["run", "${CLAUDE_PLUGIN_ROOT}/server.py"],
      "env": {
        "RESCUETIME_API_KEY": "${RESCUETIME_API_KEY}"
      }
    }
  }
}
